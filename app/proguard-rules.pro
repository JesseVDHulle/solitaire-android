# Compose + Kotlin defaults are handled by the AGP/consumer rules shipped with
# the libraries. The engine is plain Kotlin with no reflection, so nothing extra
# needs keeping here.
-dontwarn kotlinx.coroutines.**
